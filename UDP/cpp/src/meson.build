# Define the source files
src = files('logManagerClass.cpp','udpManager.cpp',)

# Declare a static library or object files (as a step towards modularity)
logging_lib = static_library('logManager', src, include_directories: inc)