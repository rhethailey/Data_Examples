#### lib_sources = [files('research_interface.cpp'), files('arm_controller.cpp'),files('udpManager.cpp')]
#### lib_sources = files('udpManager.cpp','logManagerClass.cpp')
###src = files('udpManager.cpp',)
#### src = files('udpManager.cpp','research_interface.cpp','udpManager.cpp')
###
###lib = static_library('UDP Examples',
###                     src,
###                    # include_directories: [inc, inc_custom],
###                     include_directories: inc,
###                    # cpp : cpp
###)
threads_dep = dependency('threads')

# List of source files in src/
src_sources = [
    'udpManager.cpp',
]

# Create a library for common sources
udp_lib = static_library('udpManager', src_sources, include_directories: inc_dir,dependencies: threads_dep)

# Expose the library to be used in the tools/ directory
udp_lib_dep = declare_dependency(link_with: udp_lib, include_directories, tools)
